// // changes that are made to the DOM are local to our own browsers

// window.onload = setup;

// // Function that runs when the page has loaded (It only runs once because the page is loaded once, until it's reloaded)
// function setup() {
//     console.log("running setup");
//     document.write("HELLO WORLD AFTER LOAD IN FUNCTION"); // document.write overwrites overwrites the elements written in the HTML 
// }